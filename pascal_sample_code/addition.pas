program addNumbers;

Var var_one, var_two, var_three, var_four : Integer;

begin
    var_one := 1 + 2 * 3;
    var_two := (1 + 2) * 3;
    var_three := 1 + 2 * 3 + 4 - 5;
    var_four := (1 + 2) * (3 + 4) - 5;
end.
